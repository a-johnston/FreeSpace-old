precision mediump float;
uniform mat4 uMVPMatrix;
uniform vec3 translation;

attribute vec3 position;

void main() {
	gl_PointSize = 5.0;
    gl_Position = uMVPMatrix * vec4(v_Position + translation, 1.0f);
}